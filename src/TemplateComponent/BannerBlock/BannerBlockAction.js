const loadBannerContent = (primaryTextContent, secondaryTextContent, buttonName) => ({
  type: 'LOAD_BANNER_CONTENT',
  primaryTextContent,
  secondaryTextContent,
  buttonName,
});

export default loadBannerContent;
